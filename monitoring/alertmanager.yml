global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: 'default'
    email_configs:
      - to: 'ton_email@exemple.com'
        from: 'alertmanager@booksync.local'
        smarthost: 'smtp.exemple.com:587'
        auth_username: 'alertmanager@booksync.local'
        auth_password: 'mot_de_passe'
        require_tls: true
    slack_configs:
      - api_url: 'https://hooks.slack.com/services/TON/WEBHOOK/URL'
        channel: '#booksync-alerts'
        send_resolved: true
